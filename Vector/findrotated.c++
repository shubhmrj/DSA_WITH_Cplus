#include<iostream>
#include<vector>
using namespace std;

class Solution {
public:
    bool check(vector<int>& nums) {
        int i=0;
        int j=1;
        while(j<nums.size()){
            if(nums[i]<nums[j] || count<2)
        }
    }
};

int main(){
    vector<int> nums = {2,1,3,4};
    Solution sol;
    cout<<sol.check(nums);
}